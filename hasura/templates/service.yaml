apiVersion: v1
kind: Service
metadata:
  name: {{ include "oxii-hasura-subcription.fullname" . }}
  labels:
    {{- include "oxii-hasura-subcription.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "oxii-hasura-subcription.selectorLabels" . | nindent 4 }}
