apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oxii-hasura-subcription.fullname" . }}-test-connection"
  labels:
    {{- include "oxii-hasura-subcription.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "oxii-hasura-subcription.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
